{
  "name": "sonarjs",
  "version": "0.1.0",
  "description": "the sonarjs npm module",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "test": "jest",
    "clear": "rm -rf lib/*",
    "build-mvn": "cd sonarmini && mvn clean install && cd ..",
    "copy-jars":
      "cp sonarmini/target/sonarjs-cli-*-SNAPSHOT.jar lib/ && mkdir lib/plugins && cp sonarmini/target/plugins/* lib/plugins/",
    "build":
      "yarn clear && yarn build-mvn && yarn copy-jars && yarn && tsc -p ."
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/SonarSource/sonarjs-cli.git"
  },
  "keywords": ["sonarjs", "analyzer"],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/SonarSource/sonarjs-cli/issues"
  },
  "homepage": "https://github.com/SonarSource/sonarjs-cli",
  "devDependencies": {
    "jest": "^21.2.1",
    "ts-jest": "^21.2.3",
    "typescript": "^2.6.1"
  }
}
